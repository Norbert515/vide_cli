# https://dart.dev/guides/libraries/private-files
# Created by `dart pub`
.dart_tool/

log.txt

parott
vide

# .parott/ directory is now stored globally, no longer needed in .gitignore
test_project/
packages/flutter_runtime_mcp/test_app

.env

# Local dependency overrides - don't commit pubspec.yaml with local paths
pubspec_overrides.yaml
.vscode/settings.json

# Experiments package (not ready for release)
packages/vide_experiments/
