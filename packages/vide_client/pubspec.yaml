name: vide_client
description: Dart client library for connecting to Vide servers
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  http: ^1.2.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  test: ^1.24.0
