name: agent_sdk
description: Generic interface and data model for AI coding agents
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  path: ^1.9.0

dev_dependencies:
  test: ^1.24.0
